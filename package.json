{
  "name": "tsadarsh-com",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite client",
    "build": "cd client && rm -rf ../dist && vite build --outDir ../dist",
    "start": "node server/server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.13.0",
    "three": "^0.160.0",
    "simplex-noise": "^3.0.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
